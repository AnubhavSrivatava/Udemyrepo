import json
adbfile = adb logcat > error.txt
with open ("adbfile",'r') as file :
    lst = json.load(file)
    for i in lst:
        if i = = "crash event name" :
            kill the multiproce
            then restrt the process

